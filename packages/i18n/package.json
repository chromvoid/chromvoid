{
  "name": "@project/i18n",
  "version": "0.0.1",
  "description": "Small i18n runtime built on Reatom v1000",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && node ./scripts/postbuild.mjs",
    "check:exports": "npm run build && node ./scripts/check-package-exports.mjs",
    "check:pack": "npm pack --dry-run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "prepack": "npm run build",
    "prepublishOnly": "npm run lint && npm test && npm run check:exports && npm run check:pack",
    "test": "node --import tsx --test --test-force-exit src/**/*.test.ts"
  },
  "keywords": [
    "i18n",
    "reatom",
    "translations"
  ],
  "license": "MIT",
  "dependencies": {
    "@reatom/core": "^1000.15.0"
  },
  "devDependencies": {
    "@types/node": "^24.11.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
